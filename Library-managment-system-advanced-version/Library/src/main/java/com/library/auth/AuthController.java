package com.library.auth;

/**
 * AUTH BRANCH — controller reference.
 *
 * The actual FXML controller is {@code com.library.controller.LoginController}.
 * This class exists as the branch-local entry point so that when scanning
 * the auth/ branch you know exactly which controller handles login UI.
 *
 * FXML: /com/library/ui/LoginPage.fxml
 * Real controller: com.library.controller.LoginController
 *
 * To navigate here from the tree:
 *   auth/ → AuthModule → AuthController (this file) → LoginController (FXML controller)
 */
public class AuthController {
    // Intentionally empty — see com.library.controller.LoginController for implementation.
    // This file serves as the branch-local pointer for AI scanning and documentation.
}
