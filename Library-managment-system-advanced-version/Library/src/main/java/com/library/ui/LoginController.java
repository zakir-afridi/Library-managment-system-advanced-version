package com.library.ui;

/**
 * Legacy alias — delegates everything to the new controller.
 * Kept so any old FXML files that reference this package still compile.
 */
public class LoginController extends com.library.controller.LoginController {}
